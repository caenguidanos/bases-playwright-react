import { LoginForm } from "./component";

let App: React.FunctionComponent = () => {
    return (
        <main className="m-5">
            <LoginForm />
        </main>
    );
};

export default App;
